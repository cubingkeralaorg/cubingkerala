# Database Configuration
# ======================
# 
# This file shows which environment variables are needed.
# NEVER commit actual values - use .env.local for local development
#
# Where to find actual values:
# - Local development: .env.local (gitignored)
# - Production: Vercel Dashboard → Settings → Environment Variables
# - CI/CD: GitHub → Settings → Secrets and variables → Actions

# Database
DATABASE_URL="your-database-url"

# WCA OAuth
CLIENT_ID="your-wca-client-id"
CLIENT_SECRET="your-wca-client-secret"

# Application URL (required for OAuth redirects)
# Development: http://localhost:3000
# Production: https://www.cubingkerala.org
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Test database (for CI/CD)
POSTGRES_URL_TEST="postgresql://user:password@host.region.aws.neon.tech/testdb"

# Vercel deployment secrets
VERCEL_TOKEN="your-vercel-token"
ORG_ID="your-org-id"
PROJECT_ID="your-project-id"

# Instructions:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in actual values from:
#    - Neon: https://console.neon.tech → Connection Details
#    - Vercel: https://vercel.com/account/tokens
# 3. Never commit .env.local!
